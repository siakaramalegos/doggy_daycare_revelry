<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Mardi Paws', root_path, class: 'navbar-brand' %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="<%= 'active' if current_page?(home_path) %>">
          <%= link_to 'Home', home_path %>
        </li>
        <li class="<%= 'active' if current_page?(dogs_path) %>">
          <%= link_to 'Dogs', dogs_path %>
        </li>
        <li class="<%= 'active' if current_page?(parents_path) %>">
          <%= link_to 'Parents', parents_path %>
        </li>
        <li class="<%= 'active' if current_page?(about_path) %>">
          <%= link_to 'About', about_path %>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>